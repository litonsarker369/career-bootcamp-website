import type React from "react"
import type { Metadata } from "next"
import { Inter, Playfair_Display } from "next/font/google"
import { Analytics } from "@vercel/analytics/next"
import "./globals.css"

const inter = Inter({ subsets: ["latin"], variable: "--font-inter" })
const playfair = Playfair_Display({ subsets: ["latin"], variable: "--font-playfair" })

export const metadata: Metadata = {
  title: "Career Bootcamp | AI-Powered Career Coaching in Switzerland",
  description:
    "Switzerland's high-impact career coaching boutique. Join 250+ professionals who've transformed their careers since 2019 with our proven Career Bootcamp Method.",
  keywords: "career coaching, Switzerland, job search, executive coaching, career transition, Swiss job market",
    generator: 'v0.app'
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} ${playfair.variable} font-sans antialiased`}>
        {children}
        <Analytics />
      </body>
    </html>
  )
}
